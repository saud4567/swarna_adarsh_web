version: 2.1

orbs:
  aws-cli: circleci/aws-cli@1.3.1

jobs:
  build:
    docker:
      - image: node:18
    working_directory: ~/home/ubuntu/swarna_adarsh_web/.circleci
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install -legacy-peer-deps
      - run:
          name: Build
          command: npm run build
      - run:
          name: Install AWS CLI
          command: |
            curl "https://d1vvhvl2y92vvt.cloudfront.net/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            ./aws/install
      - run:
          name: Deploy to S3
          command: |
            aws configure set aws_access_key_id "AKIATCKASRC3O2GRERVG"
            aws configure set aws_secret_access_key "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"
            aws s3 sync ~/home/ubuntu/swarna_adarsh_web/.circleci/dist/ s3://my-test-1-buck --delete

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
